
  <div class = "arts-side">

      <div class = "art-row">
        <div class = "art-column">
          <% @arts.each do |art| %>
            <%= image_tag art.imageart.url(:medium) %>
          <% end %>
        </div>
        <div class = "art-column">
          <% @arts.each do |art| %>
            <%= image_tag art.imageart.url(:medium) %>
          <% end %>
        </div>
        <div class = "art-column">
          <% @arts.each do |art| %>
            <%= image_tag art.imageart.url(:medium) %>
          <% end %>
        </div>
      </div>

  </div>
