<%= @chapters.each do |chapter| %>
 <%= chapter.imageart.url(:thumb) %>
 <%= chapter.number %>
 <%= chapter.title %>
 <% pages = ComicPage.where(comic_chapter_id: chapter.id) %>
 <% @firstPage = pages.first %>
 <%= link_to chapter.title, comic_page_side_path(@firstPage) %>
<% end %>
