</br>

  <%= image_tag @chapter.chaptercover.url(:thumb)%>
  Chapter id: <%= @chapter.id %>
  <%= @chapter.number %>
  <%= @chapter.title %>
  <% bookId = @chapter.comic_book_id %>
  <% comic = ComicBook.find_by(id: bookId) %>
  <%= comic.title %>
  </br>
  <% @pages.each do |page| %>
    <%= page.number %>
    <%= image_tag page.comicimage.url(:thumb) %>
    <%= link_to 'Edit', edit_comic_page_path(page) %>
    <%= link_to 'Show', comic_page_path(page) %>
    <%= link_to 'Delete', comic_page_path(page), method: :delete %>
  </br>
  <% end %>

<%= link_to 'Back', comic_book_path %>
